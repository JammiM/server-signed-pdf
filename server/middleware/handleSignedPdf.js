const handleSignedPdf = function (request, response, next) {
  //load the file (buffer)

  //establish file path

  //edit the file

  // write to the file

  // return html page with download link

  next();
};

export { handleSignedPdf };
